services:
  buitje:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - buitje-frames:/app/apps/backend/.data
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true

volumes:
  buitje-frames:
