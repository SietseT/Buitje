{
  "name": "buitje",
  "private": true,
  "license": "MIT",
  "packageManager": "bun@1.4.2",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"bun --filter backend dev\" \"bun --filter frontend dev --host\"",
    "dev:backend": "bun --filter backend dev",
    "dev:frontend": "bun --filter frontend dev --host",
    "build": "bun --filter frontend build && bun --filter backend build"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "workspaces": ["apps/*"]
}
